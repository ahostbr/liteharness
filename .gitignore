# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Test
.pytest_cache/
.coverage
htmlcov/
.tox/

# IDE
.vscode/
.idea/
*.swp
.DS_Store

# OS
Thumbs.db
desktop.ini

# Local
.liteeditor/
*.zip
*.log
uv.lock

# Private (personal) skills — never vendor into the public catalog (PII)
liteharness/catalog/skills/ls-discord-watch/
liteharness/catalog/skills/discord-watch/
liteharness/catalog/skills/ls-streaming-sl-obs/
liteharness/catalog/skills/streaming-sl-obs/

# Operator-internal release runbook — never vendor into the public catalog.
# See PRIVATE_SKILLS in scripts/sync_catalog.py.
liteharness/catalog/skills/ls-release-litesuite/

# Operator-internal architecture reference — never vendor into the public catalog.
# Ryan's ruling 2026-08-16. Also removed from PUBLIC_SKILLS and added to
# PRIVATE_SKILLS in scripts/sync_catalog.py; the gitignore alone would not have
# stopped the sync from vendoring it.
liteharness/catalog/skills/ls-arch-opus/

# The whole ls-arch* family is operator-internal — it reads LiteSuite's own
# docs/architecture/ tree. Ryan's ruling 2026-08-16: none of it ships until
# end-user arch docs are generated per project by that user's own Sentinel.
# Also removed from PUBLIC_SKILLS and added to PRIVATE_SKILLS in
# scripts/sync_catalog.py — the gitignore alone does not stop the vendor step.
liteharness/catalog/skills/ls-arch/
liteharness/catalog/skills/ls-arch-fable/
liteharness/catalog/skills/ls-arch-gen/
